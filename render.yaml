services:
  - type: web
    name: sajilo-sewa
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: STORAGE_DRIVER
        value: json
